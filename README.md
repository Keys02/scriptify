# Scriptify
Scriptify is a utility to create a file and make it executable at the same time and then open it in your preferred text editor

### Configuration
Put the **scriptify** file in your ~/.local/bin directory so you can run the file without specifying the absolute path.